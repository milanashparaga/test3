<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Homelibrary"
        tools:targetApi="31">
        <activity
            android:name=".presentation.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.weather.WeatherActivity" />
        <activity android:name=".presentation.auth.AuthActivity" />
        <activity android:name=".presentation.recognition.RecognitionActivity" />
        <activity android:name=".presentation.favorites.FavoritesActivity" />
        <activity android:name=".presentation.user_account.UserProfileActivity" />
        <activity android:name=".presentation.user_account.UpdateProfileActivity" />
        <activity android:name=".presentation.bookdetails.BookDetailsActivity" />
        <activity android:name=".presentation.booklist.BookListActivity" />
    </application>

</manifest>